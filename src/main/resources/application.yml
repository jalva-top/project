spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/projectdb
    username: puser
    password: 123
    driver-class-name: org.postgresql.Driver
  data:
    mongodb:
      user: muser
      password: 321
      uri: mongodb://localhost:27017/projectmdb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
